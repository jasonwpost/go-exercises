<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Hello World!</title>
  </head>
  <body>
    <ul>
      <!-- for each -->
      <!-- can also use vars in the format
           {{range $index, $element := .}}
           and {{$index}} - {{$element}}
           in the li tags, to also access
           the index ! -->
      {{range .}}
      <li>{{.}}</li>
      {{end}}
      <!-- must end the range -->
    </ul>
  </body>
</html>
